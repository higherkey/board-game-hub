---
description: SonarQube — security hotspots guidance (step 6)
---

Follow **@.agent/workflows/sonarqube-review.md** (security hotspots). **`sonar-scanner` does not** review hotspots. Give me **concrete next steps**: Sonar **UI**, **SonarQube for IDE**, or **Web API** (`/web_api` hotspot actions) if my edition/token allows—no secret values. If I should open a specific screen in SonarCloud/Server, say which.
