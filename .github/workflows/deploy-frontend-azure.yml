name: Deploy Frontend to Azure Container App

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend-azure.yml'
  workflow_dispatch:

env:
  REGISTRY: ${{ secrets.AZURE_REGISTRY_URL }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to ACR
        uses: azure/docker-login@v1
        with:
          login-server: ${{ env.REGISTRY }}
          username: ${{ secrets.AZURE_REGISTRY_USERNAME }}
          password: ${{ secrets.AZURE_REGISTRY_PASSWORD }}

      - name: Build and Push Docker Image
        run: |
          TAG=${{ github.ref_name }}
          IMAGE_NAME=${{ env.REGISTRY }}/board-game-hub-frontend:$TAG
          docker build -t $IMAGE_NAME -f frontend/Dockerfile frontend/
          docker push $IMAGE_NAME
          echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_ENV

      - name: Deploy to Azure Container Apps
        uses: azure/container-apps-deploy-action@v1
        with:
          acrName: boardgamehubacr
          # Deploy main -> 'board-game-hub-frontend', dev -> 'board-game-hub-frontend-dev'
          containerAppName: ${{ github.ref_name == 'main' && 'board-game-hub-frontend' || 'board-game-hub-frontend-dev' }}
          resourceGroup: board-game-hub-rg
          imageToDeploy: ${{ env.IMAGE_NAME }}
          targetPort: 8080
